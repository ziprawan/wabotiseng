import { readFileSync } from "node:fs";

export const stickerPath = "src/bot/stickers";
export const iya_njir = readFileSync(stickerPath + "/iya_njir.webp");
export const pola_pikir = readFileSync(stickerPath + "/pola_pikir.webp");
